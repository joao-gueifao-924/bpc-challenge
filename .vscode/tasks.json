{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Show Workspace Folder",
            "type": "shell",
            "command": "echo workspaceFolder: ${workspaceFolder}"
        }
    ]
}
